1) Leia um numero e exiba seu sucessor  cesar
#include <stdio.h>
int numero;

main(){
	
	printf("Digite um4 numero:\n");
	scanf("%i", &numero);	
	numero = numero+1;	
	printf("\nNumero sucessor: %i", numero);	
}	

2) Faça um programa em linguagem C que calcule a idade de uma pessoa, lendo o ano atual e o ano de nascimento, apresentar a idade da pessoa calculada. 

#include <stdio.h>
int idade, anoNascimento, anoAtual;
main(){
	printf("Digite o ano atual:\n");
	scanf("%i", &anoAtual);
	printf("Digite o ano de nascimento:\n");
	scanf("%i", &anoNascimento);
	idade=(anoAtual-anoNascimento);
	printf("\nIdade: %i", idade);	
}

3) Faça um programa em linguagem C que realize a conversão de um valor em dólares para real. O programa recebe o valor em dólares e a cotação, depois calcula o valor em reais

#include <stdio.h>
float dollar, cotacao, reais;
main(){
	printf("Digite o valor em dollar:\n");
	scanf("%f", &dollar);
	printf("\nDigite a cotação:\n");
	scanf("%f", &cotacao);
	reais = (dollar*cotacao);
	printf("\nValor em reais: %.2f", reais);
}


4)Faça um programa em linguagem C que calcule o pagamento de comissão de vendedores, levando-se em consideração que sua comissão será de 5% do total da venda, o usuário
ira informar o valor total da venda
#include <stdio.h>
float venda, valorComissao;
main(){
	printf("Digite o valor da venda:\n");
	scanf("%f", &venda);
	valorComissao = venda * (5.0/100.0);
	printf("\nValor da comissão: %.2f", valorComissao);
}

5)	Sabendo que um latão é constituído de 70% de cobre e 30% de zinco, indique a quantidade de cada um desses componentes para se obter uma certa quantidade de latão informada pelo usuário.
#include <stdio.h>
#include <locale.h>
float latao, cobre, zinco;
main(){
	setlocale(LC_ALL, "Portuguese");
	printf("Digite a quantidade de latão é:\n");
	scanf("%f", &latao);
	cobre = latao * (0.7);
	zinco = latao * (0.3);
	printf("\nValor cobre: %.2f valor zinco %.2f ", cobre, zinco);
}

6)	Efetuar a leitura de um número inteiro e apresentar o resultado do quadrado deste número.
#include <stdio.h>
#include <locale.h>
int numero;
main(){
	setlocale(LC_ALL, "Portuguese");
	printf("Digite um numero:\n");
	scanf("%i", &numero);
	printf("\nO quadrado deste numero é: %i", (numero*numero));
}

7)	Faça um programa que deve calcular o total da compra de um produto recebendo como informações, o valor e a quantidade deste produto. Em seguida, será informado o valor pago e o sistema deve calcular e mostrar o troco.
#include <stdio.h>
#include <locale.h>
float valorProduto, quantidade, totalCompra, valorPago, troco;
main(){
	printf("Digite o valor do produto:\n");
	scanf("%f", &valorProduto);
	printf("\nDigite a quantidade:\n");
	scanf("%f", &quantidade);
	totalCompra = valorProduto*quantidade;
	printf("\nTotal compra: %.2f", totalCompra);
	printf("\nDigite o valor pago:\n");
	scanf("%f", &valorPago);
	troco = valorPago-totalCompra;
	printf("\nTroco: %.2f", troco);
}

8)	Faça um programa que converta a temperatura de graus Celsius para graus Fahrenheit. A fórmula de conversão é: F = (9 * C + 160) / 5, onde F é a temperatura em Fahrenheit e C é a temperatura em Celsius.

9)	Ler uma temperatura em graus Fahrenheit e apresentá-la convertida em graus Celsius. A fórmula de conversão é: C = (F – 32) * (5 / 9), onde F é a temperatura em Fahrenheit e C é a temperatura em Celsius.

10) 	Ler dois valores para as variáveis A e B, efetuar a troca dos valores de forma que a variável A passe a possuir o valor da variável B e que a variável B passe a possuir o valor da variável A. Apresentar no final os valores trocados.
